//! TaskList state. Implementation lands in Task 4.

/// Mutable per-run task collection. Implementation lands in Task 4.
#[derive(Debug, Default)]
pub struct TaskList {
    _placeholder: (),
}

impl TaskList {
    /// Empty list. Implementation lands in Task 4.
    pub fn new() -> Self {
        Self::default()
    }
}
