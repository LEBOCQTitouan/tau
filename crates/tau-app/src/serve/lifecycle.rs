//! Serve lifecycle. Filled in by Task 12.

use anyhow::Result;
use super::options::ServeOptions;

/// Entry point. Stubbed; Task 12 implements.
pub async fn run(_opts: ServeOptions) -> Result<()> {
    Ok(())
}
