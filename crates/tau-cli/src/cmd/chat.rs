//! `tau chat` — open a REPL chat session with an agent.

use crate::cli::ChatArgs;
use crate::output::Output;

/// Stub: `tau chat` is implemented in Task 14.
pub async fn run(_args: &ChatArgs, _output: &mut Output) -> anyhow::Result<()> {
    anyhow::bail!("tau chat: not implemented (Task 14)")
}
